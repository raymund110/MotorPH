abstract class Calculation {

    // Abstract method to calculate the gross salary.
    // This method must be implemented by any subclass that extends this class.
    public abstract double calculateGross(double calc);

}
